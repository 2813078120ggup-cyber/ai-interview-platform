# Frontend

Run `npm install` and then `npm run dev`. The Vite development server runs at `http://localhost:5173` and proxies `/api` requests to the Spring Boot service at `http://localhost:8080`.

Use `VITE_API_BASE_URL` to point the frontend at another API address when needed.
