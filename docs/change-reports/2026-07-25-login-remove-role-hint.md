# 登录页移除角色提示文案报告

## 本次目标

移除登录页表单底部的辅助提示文案，减少视觉干扰。

## 修改内容

1. 移除提示文案
   - 删除 “管理员账号由后台初始化；候选人可直接注册并进入练习空间。” 文案。
   - 避免该提示与鼠标跟随光点、底部安全说明形成重复视觉噪音。

2. 保留核心功能
   - 登录 / 注册分段切换保留。
   - BCrypt 安全说明保留。
   - 原有登录、注册、角色跳转逻辑不变。

## 涉及文件

- `frontend-react/src/pages/login.tsx`

## 验证结果

已执行：

```bash
npm run build
```

结果：构建通过。

说明：Vite 仍提示主 JS chunk 超过 500KB，这是前端整体体量导致的既有优化项，不影响本次变更。
