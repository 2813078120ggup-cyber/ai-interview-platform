# 2026-07-24 变更报告：候选人端增加退出登录按钮

## 变更目标

在候选人端顶部右侧增加与管理员后台一致的退出登录按钮，保证不同角色的基础账号操作体验统一。

## 修改内容

1. 候选人端增加退出登录
   - 在顶部右侧工具区新增退出按钮。
   - 点击后清理本地登录会话并跳转到登录页。

2. 候选人顶部工具区与管理端统一
   - 保持主题切换、通知、退出、头像的排列方式一致。
   - 头像显示当前登录用户姓名首字，没有用户信息时显示“我”。

3. 整理候选人端外壳代码
   - 恢复侧边栏与搜索框文案为清晰中文。
   - 保留原有页面切换动画与导航逻辑。

## 影响范围

- 候选人端工作概览
- 候选人端 AI 面试大厅
- 候选人端能力报告
- 候选人端题库与岗位
- 候选人端个人中心

## 验证结果

已执行：

```bash
npm run build
```

结果：

- TypeScript 编译通过
- Vite 生产构建通过
- 存在 Vite 原有 chunk 大小提示，不影响本次功能

## 注意事项

`backend/src/main/resources/application.yml` 仍存在本地未提交改动，本次未纳入提交，避免覆盖本地运行配置。
