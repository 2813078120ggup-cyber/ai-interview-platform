import { Calendar, Clock3, Play, Search, Sparkles } from 'lucide-react'
import { useEffect, useMemo, useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { Card } from '@/components/ui/card'
import { type Interview, type PracticeBank, request } from '@/lib/api'
const labels:Record<number,string>={0:'待开始',1:'进行中',2:'已结束',3:'已取消'}
export function CandidateLobby(){const [items,setItems]=useState<Interview[]>([]),[banks,setBanks]=useState<PracticeBank[]>([]),[query,setQuery]=useState(''),[status,setStatus]=useState(''),[open,setOpen]=useState(false),[bank,setBank]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');const nav=useNavigate();useEffect(()=>{Promise.all([request<Interview[]>('/v1/interviews'),request<PracticeBank[]>('/v1/interviews/practice/banks')]).then(([a,b])=>{setItems(a);setBanks(b)}).catch(e=>setError(e.message))},[]);const list=useMemo(()=>items.filter(x=>(!status||String(x.status)===status)&&x.title.toLowerCase().includes(query.toLowerCase())),[items,query,status]);async function enter(x:Interview){try{if(x.status===0)await request(`/v1/interviews/${x.id}/start`,{method:'POST'});nav(`/candidate/interviews/${x.id}/room`)}catch(e){setError(e instanceof Error?e.message:'无法进入面试')}}async function practice(){if(!bank)return setError('请选择一个练习题库');setBusy(true);try{const result=await request<Interview>('/v1/interviews/practice',{method:'POST',body:JSON.stringify({questionBankId:bank,questionCount:5,duration:30})});nav(`/candidate/interviews/${result.id}/room`)}catch(e){setError(e instanceof Error?e.message:'创建练习失败')}finally{setBusy(false)}}return <div className="space-y-6"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="text-sm font-semibold text-emerald-600">MY AI INTERVIEWS</p><h1 className="mt-2 text-3xl font-bold">把准备，变成底气。</h1><p className="mt-2 text-muted-foreground">选择一场安排，或开始一次专属的 AI 模拟练习。</p></div><Button onClick={()=>setOpen(true)}><Sparkles className="h-4 w-4"/>开始模拟练习</Button></div>{error&&<p className="rounded-xl bg-rose-50 p-3 text-sm text-rose-700">{error}</p>}<Card><div className="flex flex-col gap-3 md:flex-row"><label className="flex h-11 flex-1 items-center gap-2 rounded-xl border border-border px-3"><Search className="h-4 w-4 text-muted-foreground"/><input className="w-full bg-transparent outline-none" placeholder="搜索面试主题" value={query} onChange={e=>setQuery(e.target.value)}/></label><select className="h-11 rounded-xl border border-border bg-surface px-3 text-sm" value={status} onChange={e=>setStatus(e.target.value)}><option value="">全部状态</option><option value="0">待开始</option><option value="1">进行中</option><option value="2">已结束</option></select></div><div className="mt-5 grid gap-4 lg:grid-cols-2">{list.map(item=><article key={item.id} className="rounded-2xl border border-border p-5 transition hover:-translate-y-0.5 hover:shadow-lg"><div className="flex justify-between"><span className="text-sm text-muted-foreground">{item.remark==='candidate-practice'?'个人模拟练习':'AI 模拟面试'}</span><Badge tone={item.status===1?'success':item.status===0?'info':'default'}>{labels[item.status]}</Badge></div><h2 className="mt-5 text-lg font-bold">{item.title}</h2><p className="mt-3 flex gap-2 text-sm text-muted-foreground"><Calendar className="h-4 w-4"/>{item.scheduledAt.replace('T',' ').slice(0,16)} <Clock3 className="ml-2 h-4 w-4"/>{item.duration} 分钟</p><div className="mt-6 flex justify-between"><span className="text-xs text-muted-foreground">#{item.id}</span>{item.status===2?<Button variant="secondary" onClick={()=>nav(`/candidate/interviews/${item.id}/report`)}>查看报告</Button>:<Button disabled={item.status===3} onClick={()=>enter(item)}><Play className="h-4 w-4"/>{item.status===1?'继续面试':'开始面试'}</Button>}</div></article>)}</div></Card>{open&&<div className="fixed inset-0 z-50 grid place-items-end bg-black/30 p-4 sm:place-items-center"><Card className="w-full max-w-lg"><h2 className="text-xl font-bold">开始模拟练习</h2><p className="mt-2 text-sm text-muted-foreground">选择题库后，AI 面试官将随机抽题并立即开始对话。</p><select className="mt-6 h-11 w-full rounded-xl border border-border bg-surface px-3" value={bank} onChange={e=>setBank(e.target.value)}><option value="">选择练习题库</option>{banks.map(x=><option key={x.id} value={x.id}>{x.name} · {x.questionCount} 题</option>)}</select><div className="mt-6 flex justify-end gap-2"><Button variant="secondary" onClick={()=>setOpen(false)}>取消</Button><Button disabled={busy} onClick={practice}>{busy?'创建中…':'立即开始'}</Button></div></Card></div>}</div>}
