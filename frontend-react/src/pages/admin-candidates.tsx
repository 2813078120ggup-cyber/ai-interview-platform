import { Plus, Search, UserCheck, UserMinus, Users, X } from 'lucide-react'
import { useEffect, useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { Card } from '@/components/ui/card'
import { request } from '@/lib/api'
type User={id:string;username:string;realName:string;email?:string;phone?:string;status:number;roles:string[];lastLoginAt?:string;createdAt?:string};type Role={id:string;roleCode:string;roleName:string};type Page<T>={records:T[];total:number}
export function AdminCandidates(){const nav=useNavigate();const [items,setItems]=useState<User[]>([]),[roles,setRoles]=useState<Role[]>([]),[keyword,setKeyword]=useState(''),[status,setStatus]=useState(''),[loading,setLoading]=useState(true),[open,setOpen]=useState(false),[error,setError]=useState(''),[saving,setSaving]=useState(false);const [form,setForm]=useState({username:'',password:'',realName:'',email:'',phone:''});async function load(){setLoading(true);try{const [page,roleList]=await Promise.all([request<Page<User>>(`/v1/users?pageNo=1&pageSize=100&keyword=${encodeURIComponent(keyword)}${status?`&status=${status}`:''}`),request<Role[]>('/v1/roles')]);setItems(page.records.filter(u=>u.roles.includes('CANDIDATE')));setRoles(roleList)}catch(e){setError(e instanceof Error?e.message:'无法加载候选人')}finally{setLoading(false)}}useEffect(()=>{void load()},[]);async function toggle(user:User){try{await request(`/v1/users/${user.id}/status`,{method:'PUT',body:JSON.stringify({status:user.status===1?0:1})});setItems(p=>p.map(x=>x.id===user.id?{...x,status:user.status===1?0:1}:x))}catch(e){setError(e instanceof Error?e.message:'状态更新失败')}}async function create(){const candidate=roles.find(r=>r.roleCode==='CANDIDATE');if(!candidate){setError('未找到 CANDIDATE 角色');return}if(!form.username||!form.password||!form.realName){setError('请填写账号、密码和姓名');return}setSaving(true);try{const user=await request<User>('/v1/users',{method:'POST',body:JSON.stringify({...form,roleIds:[candidate.id]})});setItems(p=>[user,...p]);setOpen(false);setForm({username:'',password:'',realName:'',email:'',phone:''})}catch(e){setError(e instanceof Error?e.message:'创建候选人失败')}finally{setSaving(false)}}return <div className="mx-auto max-w-7xl p-5 lg:p-9"><header className="flex flex-col gap-4 rounded-[24px] border border-border bg-surface p-5 sm:flex-row sm:items-center sm:justify-between"><div><button onClick={()=>nav('/admin/interviews')} className="text-sm text-muted-foreground hover:text-foreground">← 返回面试管理</button><p className="mt-3 text-sm font-semibold text-emerald-600">CANDIDATE DIRECTORY</p><h1 className="mt-1 text-2xl font-bold">候选人管理</h1><p className="mt-1 text-sm text-muted-foreground">统一管理候选人账号、状态和面试可用性。</p></div><Button onClick={()=>setOpen(true)}><Plus className="h-4 w-4"/>新增候选人</Button></header>{error&&<p className="mt-5 rounded-2xl border border-rose-200 bg-rose-50 px-4 py-3 text-sm text-rose-700">{error}</p>}<Card className="mt-6 p-0"><div className="flex flex-col gap-3 border-b border-border p-5 md:flex-row"><label className="flex h-11 flex-1 items-center gap-2 rounded-xl border border-border px-3"><Search className="h-4 w-4 text-muted-foreground"/><input value={keyword} onChange={e=>setKeyword(e.target.value)} onKeyDown={e=>e.key==='Enter'&&void load()} className="w-full bg-transparent text-sm outline-none" placeholder="搜索姓名、账号、邮箱或手机号"/></label><select value={status} onChange={e=>setStatus(e.target.value)} className="h-11 rounded-xl border border-border bg-surface px-3 text-sm"><option value="">全部状态</option><option value="1">已启用</option><option value="0">已停用</option></select><Button variant="secondary" onClick={()=>void load()}>搜索</Button></div>{loading?<p className="p-12 text-center text-sm text-muted-foreground">正在加载候选人…</p>:<div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="border-b border-border bg-muted/40 text-xs text-muted-foreground"><tr><th className="px-5 py-4">候选人</th><th className="px-5 py-4">联系方式</th><th className="px-5 py-4">最近登录</th><th className="px-5 py-4">状态</th><th className="px-5 py-4 text-right">操作</th></tr></thead><tbody>{items.map(u=><tr key={u.id} className="border-b border-border/70"><td className="px-5 py-5"><strong>{u.realName}</strong><p className="mt-1 text-xs text-muted-foreground">{u.username}</p></td><td className="px-5 py-5 text-muted-foreground">{u.email||u.phone||'-'}</td><td className="px-5 py-5 text-muted-foreground">{u.lastLoginAt?.replace('T',' ').slice(0,16)||'从未登录'}</td><td className="px-5 py-5"><Badge tone={u.status===1?'success':'default'}>{u.status===1?'已启用':'已停用'}</Badge></td><td className="px-5 py-5 text-right"><button onClick={()=>void toggle(u)} className="font-semibold text-emerald-600">{u.status===1?'停用':'启用'}</button></td></tr>)}{!items.length&&<tr><td colSpan={5} className="p-12 text-center text-muted-foreground">暂无候选人</td></tr>}</tbody></table></div>}</Card>{open&&<div className="fixed inset-0 z-50 bg-emerald-950/30 p-4 backdrop-blur-sm"><div className="mx-auto my-12 max-w-lg rounded-[28px] bg-surface p-6 shadow-2xl"><div className="flex justify-between"><div><p className="text-sm font-semibold text-emerald-600">NEW CANDIDATE</p><h2 className="mt-1 text-2xl font-bold">新增候选人</h2></div><button onClick={()=>setOpen(false)}><X/></button></div><div className="mt-6 grid gap-4 sm:grid-cols-2">{([['realName','姓名','刘洋'],['username','账号','candidate_liu'],['password','初始密码','至少 8 位'],['email','邮箱','name@example.com'],['phone','手机号','可选']] as const).map(([key,label,placeholder])=><label key={key} className="text-sm font-semibold">{label}<input type={key==='password'?'password':'text'} value={form[key]} onChange={e=>setForm({...form,[key]:e.target.value})} className="mt-2 h-11 w-full rounded-xl border border-border bg-background px-3 font-normal" placeholder={placeholder}/></label>)}</div><div className="mt-7 flex justify-end gap-3"><Button variant="secondary" onClick={()=>setOpen(false)}>取消</Button><Button disabled={saving} onClick={()=>void create()}>{saving?'创建中…':'创建候选人'}</Button></div></div></div>}</div>}
