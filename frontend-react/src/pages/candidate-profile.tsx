import { CalendarDays, LogOut, ShieldCheck, UserRound } from 'lucide-react'
import { useNavigate } from 'react-router-dom'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { Card } from '@/components/ui/card'
import { CandidatePageShell } from '@/components/candidate-page-shell'
import { clearSession, profile } from '@/lib/session'
export function CandidateProfile(){const nav=useNavigate();const user=profile();function logout(){clearSession();nav('/login',{replace:true})}return <CandidatePageShell><div className="max-w-4xl space-y-6"><header><p className="text-sm font-semibold text-[var(--accent)]">PERSONAL SPACE</p><h1 className="mt-2 text-3xl font-bold">个人中心</h1><p className="mt-2 text-muted-foreground">查看账号信息与当前面试权限。</p></header><Card className="overflow-hidden p-0"><div className="soft-emphasis-panel p-7"><div className="flex items-center gap-5"><span className="grid h-16 w-16 place-items-center rounded-[22px] bg-white/15 text-2xl font-bold">{user?.realName?.slice(0,1)||'我'}</span><div><h2 className="text-2xl font-bold">{user?.realName||'候选人'}</h2><p className="mt-1 text-white/75">@{user?.username}</p><div className="mt-3 flex gap-2">{user?.roles.map(role=><Badge key={role} tone="success">{role}</Badge>)}</div></div></div></div><div className="grid gap-4 p-6 sm:grid-cols-2"><div className="rounded-2xl bg-muted/60 p-4"><p className="text-sm text-muted-foreground">账号角色</p><p className="mt-2 flex items-center gap-2 font-semibold"><ShieldCheck className="h-4 w-4 text-[var(--accent)]"/>候选人练习账号</p></div><div className="rounded-2xl bg-muted/60 p-4"><p className="text-sm text-muted-foreground">可用功能</p><p className="mt-2 flex items-center gap-2 font-semibold"><CalendarDays className="h-4 w-4 text-[var(--accent)]"/>AI 面试与能力报告</p></div></div></Card><Card><div className="flex items-center justify-between"><div><h2 className="font-bold">账号安全</h2><p className="mt-1 text-sm text-muted-foreground">退出后需要重新登录才能继续访问面试内容。</p></div><Button variant="secondary" onClick={logout}><LogOut className="h-4 w-4"/>退出登录</Button></div></Card></div></CandidatePageShell>}
