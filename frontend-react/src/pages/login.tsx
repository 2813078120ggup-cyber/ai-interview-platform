import { Bot, LockKeyhole, Sparkles, UserRound } from 'lucide-react'
import { FormEvent, useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { Button } from '@/components/ui/button'
import { request } from '@/lib/api'
import { establish } from '@/lib/session'
type Login={token:string;refreshToken:string;user:{id:string;username:string;realName:string;roles:string[]}}
export function LoginPage(){const nav=useNavigate();const [mode,setMode]=useState<'login'|'register'>('login'),[busy,setBusy]=useState(false),[error,setError]=useState('');const [form,setForm]=useState({username:'',password:'',realName:'',email:'',phone:''});async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{if(mode==='register'){await request('/v1/auth/register',{method:'POST',body:JSON.stringify(form)});setMode('login');return}const result=await request<Login>('/v1/auth/login',{method:'POST',body:JSON.stringify({username:form.username,password:form.password})});establish(result.token,result.refreshToken,result.user);nav(result.user.roles.includes('ADMIN')?'/admin/interviews':'/candidate/interviews',{replace:true})}catch(reason){setError(reason instanceof Error?reason.message:'登录失败')}finally{setBusy(false)}}return <main className="grid min-h-screen bg-background lg:grid-cols-[1.05fr_.95fr]"><section className="relative hidden overflow-hidden bg-[#102925] p-12 text-white lg:flex lg:flex-col"><div className="absolute -right-24 -top-20 h-96 w-96 rounded-full bg-[var(--brand)]/20 blur-3xl"/><div className="relative flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-[var(--brand)] text-[var(--foreground)]"><Bot/></span><strong>InterviewOS</strong></div><div className="relative my-auto max-w-xl"><p className="text-sm font-semibold text-white/80">AI INTERVIEW INTELLIGENCE</p><h1 className="mt-5 text-5xl font-bold leading-[1.12]">让每一次面试，<br/>都有清晰的成长坐标。</h1><p className="mt-6 max-w-md text-lg leading-8 text-white/80/80">从 AI 对话模拟到多维能力评测，把求职准备转化为持续增长的信心。</p><div className="mt-10 flex gap-3 text-sm"><span className="rounded-full border border-white/15 bg-white/10 px-4 py-2">智能追问</span><span className="rounded-full border border-white/15 bg-white/10 px-4 py-2">能力报告</span><span className="rounded-full border border-white/15 bg-white/10 px-4 py-2">成长趋势</span></div></div></section><section className="grid place-items-center p-5 sm:p-10"><form onSubmit={submit} className="w-full max-w-md rounded-[28px] border border-border bg-surface p-7 shadow-[0_20px_60px_rgba(16,41,37,.1)]"><div className="lg:hidden"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-[var(--primary)] text-white"><Bot className="h-5 w-5"/></span></div><p className="mt-5 text-sm font-semibold text-[var(--accent)]">WELCOME TO INTERVIEWOS</p><h2 className="mt-2 text-3xl font-bold">{mode==='login'?'欢迎回来':'创建候选人账号'}</h2><p className="mt-2 text-sm text-muted-foreground">{mode==='login'?'登录后会根据你的角色进入对应工作空间。':'新注册账号默认是候选人角色。'}</p>{error&&<p className="mt-5 rounded-xl bg-rose-50 px-3 py-2 text-sm text-rose-700">{error}</p>}{mode==='register'&&<label className="mt-5 block text-sm font-semibold">姓名<input value={form.realName} onChange={e=>setForm({...form,realName:e.target.value})} className="mt-2 h-11 w-full rounded-xl border border-border bg-background px-3 font-normal" required/></label>}<label className="mt-5 block text-sm font-semibold">用户名<input value={form.username} onChange={e=>setForm({...form,username:e.target.value})} className="mt-2 h-11 w-full rounded-xl border border-border bg-background px-3 font-normal" required autoComplete="username"/></label><label className="mt-5 block text-sm font-semibold">密码<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} className="mt-2 h-11 w-full rounded-xl border border-border bg-background px-3 font-normal" required minLength={8} autoComplete={mode==='login'?'current-password':'new-password'}/></label>{mode==='register'&&<label className="mt-5 block text-sm font-semibold">邮箱（可选）<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} className="mt-2 h-11 w-full rounded-xl border border-border bg-background px-3 font-normal"/></label>}<Button className="mt-7 w-full" disabled={busy}>{busy?'处理中…':mode==='login'?'登录':'创建账号'}<Sparkles className="h-4 w-4"/></Button><button type="button" onClick={()=>{setMode(mode==='login'?'register':'login');setError('')}} className="mt-5 w-full text-sm font-semibold text-[var(--accent)] hover:text-[var(--accent)]">{mode==='login'?'没有账号？创建候选人账号':'已有账号？去登录'}</button></form></section></main>}
